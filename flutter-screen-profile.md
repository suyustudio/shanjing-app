# 我的页面（个人中心）设计

## 页面概述
用户个人中心页面，展示用户基本信息和统计数据。

---

## 布局结构

```
┌─────────────────────────────┐
│         状态栏               │
├─────────────────────────────┤
│                             │
│         [头像]               │
│                             │
│        用户昵称              │
│                             │
├─────────────────────────────┤
│                             │
│    ┌─────────────────┐      │
│    │   已走路线数     │      │
│    │                 │      │
│    │      42         │      │
│    │                 │      │
│    └─────────────────┘      │
│                             │
├─────────────────────────────┤
│                             │
│    ┌─────────────────────┐  │
│    │  ⚙️ 设置            │  │
│    └─────────────────────┘  │
│                             │
└─────────────────────────────┘
```

---

## 组件清单

### 1. 头像区域
- 圆形头像，居中显示
- 尺寸：100x100 dp
- 默认占位图或用户上传图片
- 点击可更换头像（可选）

### 2. 昵称
- 头像下方居中
- 字体：18sp，中等粗细
- 颜色：主文本色

### 3. 已走路线数卡片
- 居中显示
- 圆角卡片样式
- 标题："已走路线数"
- 数字：大号字体（32sp），强调色
- 背景：浅灰或卡片背景色

### 4. 设置入口
- 列表项样式
- 图标：⚙️ 齿轮图标
- 文字："设置"
- 右侧箭头指示
- 点击跳转设置页面

---

## 交互说明

| 元素 | 交互 | 行为 |
|------|------|------|
| 头像 | 点击 | 打开头像更换选项 |
| 设置 | 点击 | 跳转设置页面 |

---

## 颜色规范

- 背景色：`#F5F5F5`（浅灰）
- 主文本：`#212121`（深灰）
- 强调色：`#2D968A`（山青色，路线数）
- 卡片背景：`#FFFFFF`（白色）
- 分割线：`#E0E0E0`（浅灰）

---

## 极简代码结构

```dart
Scaffold(
  body: Column(
    children: [
      // 头像
      CircleAvatar(radius: 50),
      
      // 昵称
      Text('用户昵称'),
      
      // 路线数卡片
      Card(
        child: Column(
          children: [
            Text('已走路线数'),
            Text('42'), // 大号数字
          ],
        ),
      ),
      
      // 设置入口
      ListTile(
        leading: Icon(Icons.settings),
        title: Text('设置'),
        trailing: Icon(Icons.chevron_right),
      ),
    ],
  ),
)
```

---

## 未登录状态

用户未登录时显示：

```
┌─────────────────────────────┐
│         状态栏               │
├─────────────────────────────┤
│                             │
│      ┌──────────┐           │
│      │   👤     │           │  ← 默认头像
│      └──────────┘           │
│                             │
│    ┌─────────────────┐      │
│    │    点击登录      │      │  ← 登录按钮
│    └─────────────────┘      │
│                             │
│   登录后查看已走路线          │  ← 提示文案
│                             │
└─────────────────────────────┘
```

### 实现代码

```dart
// unauthenticated_state.dart
class UnauthenticatedState extends StatelessWidget {
  final VoidCallback onLoginTap;

  const UnauthenticatedState({Key? key, required this.onLoginTap}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          // 默认头像
          Container(
            width: 100,
            height: 100,
            decoration: BoxDecoration(
              color: Colors.grey[300],
              shape: BoxShape.circle,
            ),
            child: Icon(
              Icons.person_outline,
              size: 50,
              color: Colors.grey[500],
            ),
          ),
          const SizedBox(height: 24),
          
          // 登录按钮
          ElevatedButton(
            onPressed: onLoginTap,
            style: ElevatedButton.styleFrom(
              backgroundColor: Color(0xFF2D968A),
              foregroundColor: Colors.white,
              padding: EdgeInsets.symmetric(horizontal: 48, vertical: 12),
              shape: RoundedRectangleBorder(
                borderRadius: BorderRadius.circular(24),
              ),
            ),
            child: Text(
              '点击登录',
              style: TextStyle(fontSize: 16),
            ),
          ),
          const SizedBox(height: 16),
          
          // 提示文案
          Text(
            '登录后查看已走路线',
            style: TextStyle(
              fontSize: 14,
              color: Colors.grey[500],
            ),
          ),
        ],
      ),
    );
  }
}
```

### 使用方式

```dart
// profile_page.dart
@override
Widget build(BuildContext context) {
  if (!isAuthenticated) {
    return UnauthenticatedState(
      onLoginTap: () => Navigator.pushNamed(context, '/login'),
    );
  }
  // ... 正常登录状态
}
```
```
