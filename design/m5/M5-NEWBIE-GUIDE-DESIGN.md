# M5 新手引导设计稿

> **文档版本**: v1.0  
> **制定日期**: 2026-03-19  
> **关联文档**: M5-DESIGN-SPEC.md  
> **预计工时**: 10h

---

## 1. 设计概述

### 1.1 设计目标
- 帮助新用户快速了解山径 APP 核心价值
- 建立用户对产品的信任和期待
- 引导完成必要的权限授权
- 降低首次使用门槛

### 1.2 用户流程

```
首次启动 APP
    ↓
[Step 1] 欢迎页 (品牌介绍)
    ↓
[Step 2] 权限说明页
    ↓ 用户授权
[Step 3] 核心功能介绍 (3页轮播)
    - 发现路线
    - 离线导航
    - 安全求助
    ↓
进入首页 + 场景化引导
    ↓
完成引导，标记状态
```

### 1.3 设计原则

1. **简洁明快** - 单页信息量适中，快速传达核心信息
2. **视觉吸引** - 使用精美插画吸引用户注意力
3. **渐进式** - 由浅入深，逐步建立认知
4. **可跳过** - 尊重用户选择，随时可跳过
5. **温暖友好** - 文案亲切，传递品牌价值

---

## 2. 页面设计

### 2.1 Step 1 - 欢迎页

#### 布局结构

```
┌─────────────────────────────────────┐
│                                     │
│         [状态栏] 24px               │
│                                     │
│              ┌──────┐               │
│              │ LOGO │  山径图标      │
│              │ 72px │               │
│              └──────┘               │
│                                     │
│            山  径                   │
│        SHANJING APP                 │
│                                     │
│    ─────────────────────────────    │
│                                     │
│        [主视觉插图区域]              │
│          280px × 280px              │
│        山林风景 + 徒步者剪影          │
│                                     │
│    ─────────────────────────────    │
│                                     │
│        发现城市中的自然              │
│     H1 28px Bold 居中              │
│                                     │
│   探索身边的徒步路线，                │
│   享受自然，保持安全                 │
│    Body 16px 居中 60%宽度           │
│                                     │
│         [开始探索]                   │
│      主按钮 200px × 48px            │
│      圆角 24px radius-full          │
│                                     │
│         [跳过引导]                   │
│      文字按钮 14px Medium           │
│                                     │
│         [进度点 · ○ ○ ○]            │
│                                     │
│         [底部安全区域] 34px          │
└─────────────────────────────────────┘
```

#### 视觉规范

| 元素 | 规格 |
|------|------|
| 背景色 | 渐变: `#E8F5F3` → `#FAFBFB` (上到下) |
| Logo | 72px, 品牌主色 `#2D968A` |
| 品牌名 | H1 28px Bold, `#111827` |
| 副标题 | Body 16px, `#6B7280` |
| 主按钮 | 200×48px, 背景 `#2D968A`, 文字白色 |
| 跳过按钮 | 14px Medium, `#6B7280` |

#### 动效设计

**页面进入**:
```
触发: 页面首次显示
动画序列:
1. Logo: opacity 0→1, scale 0.8→1 (600ms, ease-spring, delay 0)
2. 品牌名: opacity 0→1, translateY(20px)→0 (500ms, ease-out, delay 200ms)
3. 插图: opacity 0→1, scale 0.9→1 (700ms, ease-spring, delay 400ms)
4. 副标题: opacity 0→1, translateY(20px)→0 (500ms, ease-out, delay 600ms)
5. 按钮: opacity 0→1, translateY(20px)→0 (400ms, ease-out, delay 800ms)
6. 进度点: opacity 0→1 (300ms, ease-default, delay 1000ms)
```

**按钮交互**:
```
按下: scale 0.96, 背景色 darken 10%
松开: scale 1, 触发页面切换
时长: 100ms
```

---

### 2.2 Step 2 - 权限说明页

#### 布局结构

```
┌─────────────────────────────────────┐
│                                     │
│         [状态栏] 24px               │
│                                     │
│         为了更好体验                 │
│      H1 28px Bold 居中              │
│                                     │
│    ─────────────────────────────    │
│                                     │
│        [权限说明插图]                │
│         240px × 200px               │
│      手机界面 + 权限弹窗示意         │
│                                     │
│    ─────────────────────────────    │
│                                     │
│   ┌─────────────────────────────┐   │
│   │  📍                         │   │
│   │  位置权限                   │   │
│   │  Body 14px                  │   │
│   │  用于路线导航和轨迹记录        │   │
│   │  Caption 12px #6B7280       │   │
│   └─────────────────────────────┘   │
│         卡片 边距 16px               │
│                                     │
│   ┌─────────────────────────────┐   │
│   │  💾                         │   │
│   │  存储权限                   │   │
│   │  用于保存离线地图            │   │
│   └─────────────────────────────┘   │
│                                     │
│   ┌─────────────────────────────┐   │
│   │  🔔                         │   │
│   │  通知权限                   │   │
│   │  用于安全提醒和重要通知       │   │
│   └─────────────────────────────┘   │
│                                     │
│                                     │
│    [允许全部权限]  [稍后设置]        │
│     主按钮          次按钮          │
│                                     │
│    你可以在设置中随时更改权限        │
│         Caption 12px #9CA3AF       │
│                                     │
│         [进度点 ○ · ○ ○]            │
│                                     │
│         [底部安全区域] 34px          │
└─────────────────────────────────────┘
```

#### 权限卡片设计

```
卡片规格:
- 宽度: 100% - 48px (边距 24px)
- 高度: 自适应 (最小 72px)
- 背景: #FFFFFF
- 圆角: 12px (radius-lg)
- 阴影: shadow-md
- 内边距: 16px

内部布局:
┌────────────────────────────────────┐
│ ┌────┐ ┌────────────────────────┐  │
│ │icon│ │ 权限名称               │  │
│ │48px│ │ H4 17px Semibold       │  │
│ │    │ │                        │  │
│ │ 🌍 │ │ 权限说明               │  │
│ └────┘ │ Caption 12px #6B7280   │  │
│        └────────────────────────┘  │
└────────────────────────────────────┘
```

#### 权限图标

| 权限 | 图标 | 背景色 | 图标色 |
|------|------|--------|--------|
| 位置 | map-pin | `#E8F5F3` | `#2D968A` |
| 存储 | hard-drive | `#FFF8E7` | `#FFB800` |
| 通知 | bell | `#EEF7FF` | `#3B9EFF` |

#### 按钮规范

```
主按钮 [允许全部权限]:
- 宽度: 160px
- 高度: 48px
- 背景: #2D968A
- 文字: 白色 16px Semibold
- 圆角: 24px

次按钮 [稍后设置]:
- 宽度: 120px
- 高度: 48px
- 背景: transparent
- 文字: #6B7280 16px Medium
- 边框: 1px solid #E5E7EB
- 圆角: 24px
```

#### 动效设计

**卡片依次进入**:
```
触发: 页面显示
动画: opacity 0→1, translateX(-30px)→0
时长: 400ms
缓动: ease-out
错开: 150ms (卡片1 delay 0, 卡片2 delay 150ms, 卡片3 delay 300ms)
```

**权限请求流程**:
```
用户点击 [允许全部权限]
    ↓
显示系统权限弹窗 (iOS/Android 原生)
    ↓
权限结果回调
    ↓
卡片状态更新:
- 已授权: 图标变绿 ✅, 背景色变为 #E8F5F3
- 被拒绝: 图标变灰, 显示 [去设置] 按钮
    ↓
所有必需权限已授权 OR 用户点击 [下一步]
    ↓
进入 Step 3
```

---

### 2.3 Step 3 - 核心功能介绍

Step 3 包含 3 个子页面，使用 PageView 左右滑动切换。

#### 3a. 发现路线

```
┌─────────────────────────────────────┐
│                                     │
│         [状态栏] 24px               │
│                                     │
│    ─────────────────────────────    │
│                                     │
│        [功能介绍插图]                │
│         280px × 280px               │
│      地图 + 路线标记 + 风景插画       │
│                                     │
│    ─────────────────────────────    │
│                                     │
│          发现路线                    │
│      H1 28px Bold 居中              │
│                                     │
│   探索身边的徒步路线                  │
│   发现城市中的自然之美                │
│     Body 16px 居中 #6B7280          │
│                                     │
│      ┌─────────────────────┐        │
│      │   [下一步 →]         │        │
│      │   按钮 200×48px      │        │
│      └─────────────────────┘        │
│                                     │
│         [跳过引导]                   │
│                                     │
│         [进度点 ○ ○ · ○]            │
│                                     │
│         [底部安全区域] 34px          │
└─────────────────────────────────────┘
```

#### 3b. 离线导航

```
┌─────────────────────────────────────┐
│                                     │
│         [状态栏] 24px               │
│                                     │
│    ─────────────────────────────    │
│                                     │
│        [功能介绍插图]                │
│         280px × 280px               │
│      无信号图标 + 离线地图示意        │
│      显示 "离线模式可用" 标签         │
│                                     │
│    ─────────────────────────────    │
│                                     │
│          离线导航                    │
│      H1 28px Bold 居中              │
│                                     │
│    没信号也能安心走                  │
│    提前下载，山野无忧                 │
│     Body 16px 居中 #6B7280          │
│                                     │
│      ┌─────────────────────┐        │
│      │   [下一步 →]         │        │
│      └─────────────────────┘        │
│                                     │
│         [跳过引导]                   │
│                                     │
│         [进度点 ○ ○ ○ ·]            │
│                                     │
└─────────────────────────────────────┘
```

#### 3c. 安全求助

```
┌─────────────────────────────────────┐
│                                     │
│         [状态栏] 24px               │
│                                     │
│    ─────────────────────────────    │
│                                     │
│        [功能介绍插图]                │
│         280px × 280px               │
│      SOS按钮 + 紧急联系人示意         │
│                                     │
│    ─────────────────────────────    │
│                                     │
│          安全求助                    │
│      H1 28px Bold 居中              │
│                                     │
│    一键求助，守护安全                │
│    让爱你的人不再担心                 │
│     Body 16px 居中 #6B7280          │
│                                     │
│      ┌─────────────────────┐        │
│      │    [开始使用 →]      │        │
│      │   按钮 200×48px      │        │
│      │   背景 #2D968A       │        │
│      └─────────────────────┘        │
│                                     │
│         [进度点 ○ ○ ○ ○]             │
│                                     │
└─────────────────────────────────────┘
```

#### 页面切换动效

```
触发: 滑动或点击按钮
动画: PageView 滑动过渡
- 当前页: translateX(0) → translateX(-100%)
- 下一页: translateX(100%) → translateX(0)
时长: 400ms
缓动: ease-out

页面内元素:
- 插图: 保持位置，仅内容变化
- 文字: 淡入淡出 opacity 切换 (200ms)
- 进度点: 平滑过渡宽度和颜色 (300ms)
```

---

## 3. 场景化引导

### 3.1 首页高亮引导

完成引导流程进入首页后，显示场景化引导：

```
┌─────────────────────────────────────┐
│                                     │
│         [顶部导航栏]                 │
│                                     │
├─────────────────────────────────────┤
│ ╔═════════════════════════════════╗ │
│ ║                                 ║ │
│ ║   [高亮区域 - 遮罩挖空]           ║ │
│ ║                                 ║ │
│ ║   ┌─────────────────────────┐   ║ │
│ ║   │  🏔️ 九溪十八涧          │   ║ │ ← 高亮
│ ║   │     3.2km · 休闲        │   ║ │
│ ║   └─────────────────────────┘   ║ │
│ ║                                 ║ │
│ ╚═════════════════════════════════╝ │
│         ↑ 遮罩层 (rgba(0,0,0,0.7))   │
│                                     │
│   ┌─────────────────────────────┐   │
│   │                             │   │
│   │     👆                      │   │
│   │   点击这里发现附近路线        │   │
│   │                             │   │
│   │   [我知道了]                │   │
│   │                             │   │
│   └─────────────────────────────┘   │
│      引导提示卡片                   │
│                                     │
│  [其他路线内容...]                   │
│                                     │
└─────────────────────────────────────┘
```

### 3.2 遮罩实现方式

```dart
// 遮罩层实现
Stack(
  children: [
    // 底层: 原页面内容
    HomePage(),
    
    // 遮罩层: 黑色半透明 + 挖空
    ColorFiltered(
      colorFilter: ColorFilter.mode(
        Colors.black54,
        BlendMode.srcOut,
      ),
      child: Stack(
        children: [
          Container(color: Colors.black),
          // 挖空区域 (高亮位置)
          Positioned(
            top: 120,
            left: 24,
            child: Container(
              width: 327,
              height: 80,
              decoration: BoxDecoration(
                color: Colors.white, // 挖空显示白色
                borderRadius: BorderRadius.circular(12),
              ),
            ),
          ),
        ],
      ),
    ),
    
    // 提示卡片
    Positioned(
      top: 220,
      left: 48,
      child: GuideCard(),
    ),
  ],
)
```

### 3.3 引导提示卡片

```
卡片规格:
- 宽度: 280px
- 背景: #FFFFFF
- 圆角: 16px
- 阴影: shadow-lg
- 内边距: 20px

内容布局:
┌────────────────────────────────┐
│                                │
│          👆                    │
│        36px 图标                │
│                                │
│    点击这里发现附近路线          │
│    Body 16px Medium 居中       │
│                                │
│    ┌────────────────────┐      │
│    │     我知道了        │      │
│    │   按钮 120×40px    │      │
│    └────────────────────┘      │
│                                │
└────────────────────────────────┘
```

---

## 4. 进度指示器

### 4.1 进度点设计

```
规格:
- 点直径: 8px (默认) / 24px (当前)
- 点间距: 8px
- 默认颜色: #D1D5DB (gray-300)
- 当前颜色: #2D968A (primary-500)

布局:
[ ●  ─  ○  ─  ○  ─  ○ ]
  ↑当前   未激活

动效:
- 过渡: width 8px→24px, borderRadius 4px→12px
- 时长: 300ms
- 缓动: ease-default
```

### 4.2 页面计数器 (可选)

```
样式: "1 / 4"
字体: Caption 12px Medium
颜色: #9CA3AF
位置: 底部居中，进度点上方 8px
```

---

## 5. 暗黑模式适配

### 5.1 欢迎页暗黑模式

| 元素 | 浅色模式 | 深色模式 |
|------|----------|----------|
| 背景 | 渐变 #E8F5F3 → #FAFBFB | 渐变 #0F1419 → #1A1F24 |
| 品牌名 | #111827 | #FFFFFF |
| 副标题 | #6B7280 | #9CA3AF |
| 按钮 | #2D968A | #4DB6AC |
| 插图 | 正常色彩 | 饱和度 -20% |

### 5.2 权限页暗黑模式

| 元素 | 浅色模式 | 深色模式 |
|------|----------|----------|
| 背景 | #FAFBFB | #0F1419 |
| 卡片背景 | #FFFFFF | #1A1F24 |
| 卡片边框 | 无 | 1px #2D3748 |
| 权限名称 | #111827 | #FFFFFF |
| 权限说明 | #6B7280 | #9CA3AF |
| 次按钮边框 | #E5E7EB | #4B5563 |

### 5.3 功能介绍页暗黑模式

与欢迎页一致，插图使用降低饱和度的版本。

---

## 6. 响应式适配

### 6.1 小屏适配 (iPhone SE / 小屏 Android)

```
调整项:
- 插图尺寸: 280px → 240px
- 标题字号: 28px → 24px
- 页面边距: 32px → 24px
- 按钮高度: 48px → 44px
- 垂直间距按比例缩小 20%
```

### 6.2 大屏适配 (iPhone Pro Max / 大屏 Android)

```
调整项:
- 插图尺寸: 280px → 320px
- 最大内容宽度: 375px (居中)
- 按钮宽度: 200px → 240px
- 保持垂直居中布局
```

---

## 7. 插画规范

### 7.1 插画风格

- **风格**: 扁平插画 + 微渐变
- **色彩**: 使用品牌色板，饱和度适中
- **线条**: 2px 描边，圆角端点
- **人物**: 简洁几何风格，无面部细节

### 7.2 欢迎页插画

**主题**: 山林晨曦
- 远山剪影 (深蓝绿色)
- 中景树林 (品牌绿色)
- 徒步者剪影 (小号，点缀)
- 日出/晨雾效果 (渐变)

### 7.3 权限页插画

**主题**: 权限授权示意
- 手机轮廓 (简约线条)
- 权限弹窗 (半透明覆盖)
- 图标元素 (位置、存储、通知图标)

### 7.4 功能介绍插画

| 页面 | 主题 | 元素 |
|------|------|------|
| 发现路线 | 地图探索 | 地图、路线、地标、指南针 |
| 离线导航 | 无信号导航 | 手机、离线地图、无信号图标、山林 |
| 安全求助 | 紧急求助 | SOS按钮、紧急联系人、安心符号 |

---

## 8. 交互状态

### 8.1 按钮状态

```
主按钮 [开始探索]:

默认状态:
- 背景: #2D968A
- 文字: 白色
- 阴影: shadow-md

按下状态:
- 背景: #25877C (darken 10%)
- scale: 0.96
- 阴影: shadow-sm

禁用状态:
- 背景: #9DD6CE (primary-200)
- 文字: rgba(255,255,255,0.6)
```

### 8.2 跳过引导

```
行为:
- 点击后显示确认弹窗:
  "确定跳过引导？"
  "你可以随时在设置中重新查看"
  [取消] [确定跳过]

- 确认后:
  1. 标记引导完成
  2. 进入首页
  3. 显示 toast: "可随时在设置中查看引导"
```

### 8.3 返回操作

```
Android 物理返回键:
- Step 1: 退出 APP
- Step 2+: 返回上一页

iOS 滑动返回:
- 支持从左边缘滑动返回上一页
- Step 1 不支持返回
```

---

## 9. 动效详细规范

### 9.1 页面切换动效

```dart
// PageView 转场动画
PageView(
  physics: BouncingScrollPhysics(),
  onPageChanged: (index) {
    // 更新进度点
  },
  children: [
    AnimatedBuilder(
      animation: pageController,
      builder: (context, child) {
        double value = 1.0;
        if (pageController.position.haveDimensions) {
          value = pageController.page! - index;
          value = (1 - (value.abs() * 0.3)).clamp(0.0, 1.0);
        }
        return Opacity(
          opacity: value,
          child: Transform.translate(
            offset: Offset(0, 50 * (1 - value)),
            child: child,
          ),
        );
      },
      child: OnboardingPage(),
    ),
  ],
)
```

### 9.2 元素进入序列

```
每个页面元素的进入延迟:

Step 1 欢迎页:
- 0ms: 品牌名
- 200ms: Logo
- 400ms: 插图
- 600ms: 副标题
- 800ms: 主按钮
- 1000ms: 跳过按钮
- 1200ms: 进度点

Step 2 权限页:
- 0ms: 标题
- 200ms: 插图
- 400ms: 权限卡片1
- 550ms: 权限卡片2
- 700ms: 权限卡片3
- 900ms: 按钮组

Step 3 功能介绍:
- 0ms: 插图
- 200ms: 标题
- 400ms: 描述
- 600ms: 按钮
```

---

## 10. 验收标准

### 10.1 功能验收

- [ ] 首次安装启动显示引导流程
- [ ] 可随时点击 [跳过引导]
- [ ] 支持左右滑动切换页面
- [ ] Step 2 权限申请不阻塞流程
- [ ] 完成引导后不再显示
- [ ] 支持在设置中重新查看引导

### 10.2 设计验收

- [ ] 所有页面符合设计稿规范
- [ ] 动画流畅，无明显卡顿
- [ ] 暗黑模式正确切换
- [ ] 小屏/大屏适配正常
- [ ] 图片资源清晰无失真

### 10.3 性能验收

- [ ] 页面切换 60fps
- [ ] 首屏加载 < 500ms
- [ ] 图片预加载，无闪烁
- [ ] 内存占用合理

---

## 11. 附录

### 11.1 文案汇总

| 位置 | 文案 |
|------|------|
| 欢迎页标题 | 发现城市中的自然 |
| 欢迎页副标题 | 探索身边的徒步路线，享受自然，保持安全 |
| 权限页标题 | 为了更好体验 |
| 位置权限 | 用于路线导航和轨迹记录 |
| 存储权限 | 用于保存离线地图 |
| 通知权限 | 用于安全提醒和重要通知 |
| 发现路线标题 | 发现路线 |
| 发现路线描述 | 探索身边的徒步路线，发现城市中的自然之美 |
| 离线导航标题 | 离线导航 |
| 离线导航描述 | 没信号也能安心走，提前下载，山野无忧 |
| 安全求助标题 | 安全求助 |
| 安全求助描述 | 一键求助，守护安全，让爱你的人不再担心 |
| 场景化引导 | 点击这里发现附近路线 |

### 11.2 资源清单

```
assets/onboarding/
├── welcome_illustration.svg
├── permission_illustration.svg
├── feature_discover.svg
├── feature_offline.svg
├── feature_safety.svg
└── guide_spotlight.svg
```

---

*本文档由 Design Agent 创建，详细定义了 M5 新手引导的视觉和交互设计。*
