name: Build and Release APK (Fixed)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'
        channel: 'stable'
    
    - name: Check and create Android platform
      run: |
        if [ ! -d "android" ]; then
          echo "Android directory not found, creating..."
          flutter config --enable-android
          flutter create --platforms=android --project-name=shanjing_app .
        else
          echo "Android directory already exists"
        fi
    
    - name: Update Kotlin version
      run: |
        echo "=== Current android/app/build.gradle ==="
        cat android/app/build.gradle | head -30
        echo ""
        echo "=== Updating Kotlin version ==="
        # Kotlin 版本在 android/app/build.gradle 中的 plugins 部分
        if grep -q "kotlin("android")" android/app/build.gradle; then
          sed -i 's/kotlin("android") version "[^"]*"/kotlin("android") version "1.9.0"/' android/app/build.gradle
          echo "Updated kotlin plugin version"
        else
          echo "WARNING: Could not find kotlin plugin pattern"
        fi
        echo ""
        echo "=== Updated android/app/build.gradle ==="
        cat android/app/build.gradle | head -30
    
    - name: Get dependencies
      run: flutter pub get
    
    - name: Generate keystore
      run: |
        mkdir -p android/app
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/shanjing.keystore
    
    - name: Configure key.properties
      run: |
        cat > android/key.properties << EOL
        storePassword=${{ secrets.KEYSTORE_PASSWORD }}
        keyPassword=${{ secrets.KEY_PASSWORD }}
        keyAlias=shanjing
        storeFile=shanjing.keystore
        EOL
    
    - name: Build APK
      run: flutter build apk --release
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: shanjing-apk
        path: build/app/outputs/flutter-apk/*.apk
        if-no-files-found: error
